services:
  nginx:
    image: nginx:stable-alpine
    container_name: nginx_proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./backend/static:/usr/share/nginx/html:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - fast1
    networks:
      - backend-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  fast1:
    image: fast1
    container_name: fast1_app
    restart: always
    expose:
      - "8000"
    volumes:
      - ./backend/env.py:/code/env.py:Z
      - ./backend/static:/code/static:ro
    depends_on:
      - redis-db
    networks:
      - backend-network

  worker:
    image: fast1
    container_name: fast1_worker
    restart: always
    command: arq worker.WorkerSettings
    volumes:
      - ./backend/env.py:/code/env.py:Z
    depends_on:
      - redis-db
    networks:
      - backend-network

  redis-db:
    image: redis:7-alpine
    container_name: redis_server
    restart: always
    networks:
      - backend-network

networks:
  backend-network:
    driver: bridge
